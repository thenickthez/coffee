<?php
require_once __DIR__.'/../config/auth.php';if(!empty($_SESSION['admin_id'])){header('Location:index.php');exit;}$error='';if($_SERVER['REQUEST_METHOD']==='POST'){$u=trim($_POST['username']??'');$p=$_POST['password']??'';$s=db()->prepare("SELECT * FROM admins WHERE username=?");$s->execute([$u]);$a=$s->fetch();if($a&&password_verify($p,$a['password_hash'])){$_SESSION['admin_id']=$a['id'];header('Location:index.php');exit;}$error='Invalid username or password.';}
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin Login</title><link rel="stylesheet" href="../assets/style.css"></head><body class="login"><form class="login-box" method="post"><span class="eyebrow">ADMIN</span><h1 style="font-size:42px">Sign in.</h1><?php if($error):?><div class="notice"><?=$error?></div><?php endif;?><div class="form-row"><label>Username</label><input name="username" required></div><div class="form-row"><label>Password</label><input type="password" name="password" required></div><button class="btn btn-yellow" style="width:100%">SIGN IN</button></form></body></html>
